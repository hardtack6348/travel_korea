spring:
#  config:
#    import: optional:file:.env
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://192.168.0.192:3306/xe
    url: ${secrets.DB_URL}
    username: scott
    password: ${secrets.DB_PW}
#    password: TigerTiger1$
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  application:
    name: travel_korea
server:
  port: 8080